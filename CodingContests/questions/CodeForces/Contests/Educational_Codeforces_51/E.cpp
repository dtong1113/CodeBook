#include <bits/stdc++.h>

using namespace std;

int mod = 998244353;

bool comp(string& g) {
  if(g.size() < r.size()) {

  }

}
int main() {
  //ifstream cin("input.txt");
  //ofstream cout("out.txt");
  
  string s;
  cin >> s;
  int req =
  if(s[0] == '0') {
    cout << '0';
    return 0;
  }

  return 0;
}
